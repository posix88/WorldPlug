# Copy this file to fastlane/.env (gitignored) and fill in real values, or export these as
# shell environment variables instead — either works. See fastlane/README.md for how to get
# these from App Store Connect.

ASC_KEY_ID=
ASC_ISSUER_ID=
ASC_KEY_FILEPATH=
